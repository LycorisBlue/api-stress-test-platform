export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-3xl font-bold">API Load & Stress Test — UI</h1>
        <p className="mt-2 text-gray-600">Sprint 0: UI prête — Orchestrateur /health</p>
      </div>
    </main>
  );
}
